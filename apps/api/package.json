{
  "name": "@ices/api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "biome check .",
    "format": "biome format .",
    "clean": "rm -rf dist",
  "test": "vitest run"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongodb": "^6.8.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^20",
    "@types/express": "^4",
    "vitest": "^2.1.3",
    "supertest": "^7.0.0",
    "@types/supertest": "^2.0.16"
  }
}
